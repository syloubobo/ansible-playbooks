---
- name: Alignement complet Veyon sur Linux Mint
  hosts: all
  become: yes
  gather_facts: no

  tasks:
    - name: Installation du configurateur graphique et de ses dépendances
      apt:
        name:
          - veyon-service
          - veyon-configurator
        state: present
        update_cache: yes

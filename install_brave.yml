---
- name: Deploiement automatise du navigateur Brave
  hosts: all
  become: true

  tasks:
    - name: Installation des dependances requises (curl)
      apt:
        name: curl
        state: present
        update_cache: yes

    - name: Telechargement et ajout de la cle GPG officielle de Brave
      get_url:
        url: https://brave-browser-apt-release.s3.brave.com/brave-browser-archive-keyring.gpg
        dest: /usr/share/keyrings/brave-browser-archive-keyring.gpg
        mode: '0644'

    - name: Ajout du depot officiel Brave Browser dans les sources APT
      apt_repository:
        repo: "deb [signed-by=/usr/share/keyrings/brave-browser-archive-keyring.gpg] https://brave-browser-apt-release.s3.brave.com/ stable main"
        state: present
        filename: brave-browser-release

    - name: Mise a jour du cache et installation du paquet brave-browser
      apt:
        name: brave-browser
        state: latest
        update_cache: yes

    - name: Obtenir la liste de tous les utilisateurs
      getent:
        database: passwd
        key: "*"
      changed_when: false
      register: users_data

    - name: Creer le dossier Desktop pour chaque utilisateur
      file:
        path: "/home/{{ item }}/Desktop"
        state: directory
        owner: "{{ item }}"
        group: "{{ item }}"
        mode: '0755'
      loop: "{{ users_data.ansible_facts.getent_passwd.keys() | list }}"
      when: users_data.ansible_facts.getent_passwd[item][4] | int >= 1000

    - name: Copier le fichier .desktop de Brave vers le bureau de chaque utilisateur
      copy:
        src: /usr/share/applications/brave-browser.desktop
        dest: "/home/{{ item }}/Desktop/brave-browser.desktop"
        remote_src: yes
        owner: "{{ item }}"
        group: "{{ item }}"
        mode: '0755'
      loop: "{{ users_data.ansible_facts.getent_passwd.keys() | list }}"
      when: users_data.ansible_facts.getent_passwd[item][4] | int >= 1000
      ignore_errors: yes

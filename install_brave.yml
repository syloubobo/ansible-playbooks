---
- name: Deploiement automatise du navigateur Brave
  hosts: all
  become: true

  tasks:
    - name: Installation des dependances requises (curl)
      apt:
        name: curl
        state: present
        update_cache: yes

    - name: Telechargement et ajout de la cle GPG officielle de Brave
      get_url:
        url: https://brave-browser-apt-release.s3.brave.com/brave-browser-archive-keyring.gpg
        dest: /usr/share/keyrings/brave-browser-archive-keyring.gpg
        mode: '0644'

    - name: Ajout du depot officiel Brave Browser dans les sources APT
      apt_repository:
        repo: "deb [signed-by=/usr/share/keyrings/brave-browser-archive-keyring.gpg] https://brave-browser-apt-release.s3.brave.com/ stable main"
        state: present
        filename: brave-browser-release

    - name: Mise a jour du cache et installation du paquet brave-browser
      apt:
        name: brave-browser
        state: latest
        update_cache: yes

    - name: Trouver tous les utilisateurs
      find:
        paths: /home
        file_type: directory
        recurse: no
      register: home_dirs

    - name: Creer le dossier Desktop pour chaque utilisateur
      file:
        path: "{{ item.path }}/Desktop"
        state: directory
        owner: "{{ item.path | basename }}"
        group: "{{ item.path | basename }}"
        mode: '0755'
      loop: "{{ home_dirs.files }}"

    - name: Creer le raccourci Brave sur le bureau
      shell: |
        cat > {{ item.path }}/Desktop/brave-browser.desktop << 'EOF'
        [Desktop Entry]
        Version=1.0
        Type=Application
        Name=Brave Browser
        Comment=Accedez a Internet avec Brave
        Exec=/usr/bin/brave-browser %U
        Icon=brave-browser
        Terminal=false
        Categories=Network;WebBrowser;
        MimeType=text/html;text/xml;application/xhtml+xml;x-scheme-handler/http;x-scheme-handler/https;
        StartupNotify=true
        EOF
        chown {{ item.path | basename }}:{{ item.path | basename }} {{ item.path }}/Desktop/brave-browser.desktop
        chmod 755 {{ item.path }}/Desktop/brave-browser.desktop
      loop: "{{ home_dirs.files }}"
